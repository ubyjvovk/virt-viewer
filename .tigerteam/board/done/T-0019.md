---
id: T-0019
title: CI — make the macOS app-bundle upload step reachable
priority: P2
complexity: C1
depends_on: []
scope: [.github/workflows/macos.yml]
---

## Goal
The workflow's "Upload app bundle" step can never run: nothing invokes
`make-bundle.sh`, so `hashFiles('build/*.app/Contents/Info.plist')` is
always empty and the step is permanently skipped (`dylibbundler` is
installed for that never-taken path). Found independently by two stress
reviews (T-0013 N1, T-0012 N4). PM decision: build the bundle in CI rather
than dropping the step — it exercises the packaging path on every push.

## Context
`.github/workflows/macos.yml`. The build step runs
`bash build-aux/macos/check.sh`; `build-aux/macos/make-bundle.sh` expects
the meson build in `build/` (the default) and produces
`build/Remote Viewer.app`.

## Required change
Add one step between "Build and test" and "Upload meson logs":

    - name: Build app bundle
      run: bash build-aux/macos/make-bundle.sh

Keep the existing upload steps and the brew dependency list unchanged.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (unaffected; run per
      protocol).
- [ ] `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/macos.yml'))"`
      exits 0.
- [ ] `grep -n 'make-bundle' .github/workflows/macos.yml` shows the new
      step, ordered after the check.sh step and before the log upload.
- [ ] Documentation: none — the CI caveat sentence in docs is T-0018's.
- [ ] No files changed outside scope.

## Out of scope
Running the workflow (no GitHub remote CI from this board). Changing
check.sh or make-bundle.sh. Docs.

## Worker report

**Files changed (scope `.github/workflows/macos.yml` only):**
- `.github/workflows/macos.yml` — added the "Build app bundle" step
  (`run: bash build-aux/macos/make-bundle.sh`) between the "Build and test"
  step and the "Upload meson logs" step, so the "Upload app bundle" step's
  `hashFiles('build/*.app/Contents/Info.plist')` guard is no longer always
  empty and the packaging path runs on every push. Existing upload steps and
  the brew dependency list are unchanged.

**Commands run / test summary:**
- `bash .tigerteam/scripts/run-tests.sh` → exit 0; `Fail: 0`; `Ok: 4`
  (test-version-compare, test-monitor-mapping, test-hotkeys,
  test-monitor-alignment).
- `python3 -c "import yaml; yaml.safe_load(...)"` → exit 0 (YAML valid; run in a
  temp venv because PyYAML is not in the host python — see note).
- `grep -n 'make-bundle' .github/workflows/macos.yml` → step at lines 28-29,
  after "Build and test" (25) and before "Upload meson logs" (31).

**Doc changes:** none (ticket states docs are T-0018's; the CI caveat
sentence in docs is not in scope).

**Commit:** `ba715f6` on branch `tigerteam/T-0019`.

**For the reviewer:**
- Acceptance criteria all met: tests exit 0, YAML parses, step ordering
  correct, only scope file changed (verified via `git status --short`).
- Note: the host `python3` (Homebrew 3.14, externally managed) has no
  PyYAML, so I ran the forced YAML-check in a throwaway venv under `/tmp`
  (PEP 668 prevented `pip install --user`). The venv is outside the
  worktree and not committed.
- Out of scope / not touched: workflow not run (no GitHub remote CI from
  this board); check.sh and make-bundle.sh unchanged.

> claimed_by: ds-3 (pid 66378) at 2026-08-31T17:32:12Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-3 | deepseek-ai/DeepSeek-V4-Flash-0731 | 1m 12s | 193.76k (176.13k) | 3.11k | — | — | review |

total: 1 attempt · 1m 12s · 193.76k in (176.13k cached) / 3.11k out · $0.00 · 1 without cost (accepted 2026-08-31T17:35:04Z)
