---
id: T-0011
title: PR series — mac-port-pr, upstream macOS port (8 commits, PM-assembled)
priority: P1
complexity: C3
depends_on: []
scope: [src/, build-aux/macos/, docs/, man/, meson.build, meson_options.txt, config.h.in, po/POTFILES, .github/, .gitignore, README.md]
assignee: pm
---

## Goal
Synthetic target ticket for the assembled upstream merge-request branch
`mac-port-pr` (tip mirrored as `tigerteam/T-0011`). It exists so review
tickets (`review_of: T-0011`) can check out and stress-review the exact
series that will be pushed to gitlab.com/virt-viewer/virt-viewer.

## Context
- 8 commits over `master`: build meson / bundle paths / cmd+meta hotkeys /
  macos integration module / URL+.vv handlers / packaging / docs / CI.
- Assembled by the PM from the drained board (T-0001..T-0010) on
  2026-08-31; util.c, window.c, src/meson.build split at hunk level.
- Deviations from `mac-port` (intentional, 4): .gitignore trimmed,
  check.sh comment de-tigerteamed, docs/macos.md drops an internal QA
  path, po/POTFILES gains src/virt-viewer-macos.c.

## Acceptance criteria
- [ ] Stress reviews (T-0012, T-0013, T-0014) accepted and their findings
      triaged by the PM.

## Out of scope
Implementation. This ticket carries no worker attempt; it is the review
target only.

## Worker report

### PM assembly report (in lieu of a worker attempt)
Every code commit (1–5) was built and tested on the host at assembly
time: `bash build-aux/macos/check.sh` → 4/4 meson tests green each time.
`ninja virt-viewer-pot` validates the POTFILES addition; the regenerated
pot was deliberately not committed (Weblate-managed churn).
`git diff mac-port mac-port-pr` (product paths) shows exactly the four
intentional deviations listed in Context.
