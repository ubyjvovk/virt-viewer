---
id: T-0008
title: Homebrew formula + DMG target for the mac port
priority: P3
complexity: C2
depends_on: [T-0004]
scope: [build-aux/macos/virt-viewer.rb, build-aux/macos/make-dmg.sh, docs/macos.md]
---

## Goal
Two distribution helpers: a Homebrew formula (`head` build from this
repository, usable as a local tap) and a script that wraps
`Remote Viewer.app` into a compressed DMG.

## Context
- Formula: `build-aux/macos/virt-viewer.rb`, class `VirtViewer`, `head`
  pointing at the repository's git URL with `branch: "mac-port"` (read the
  URL from `git remote get-url origin`), depends_on for the brew packages
  listed in `docs/macos.md`, `def install` = `system "meson", "setup",
  "build", *std_meson_args` / `ninja -C build` / `ninja -C build install`,
  and a `test do` running `remote-viewer --version`. Validate with
  `brew audit --formula build-aux/macos/virt-viewer.rb` if it runs offline;
  otherwise `ruby -c`.
- DMG: `build-aux/macos/make-dmg.sh` (bash, `set -euo pipefail`) uses
  `hdiutil create -volname "Remote Viewer" -srcfolder <staging> -ov -format UDZO
  <out.dmg>` where staging contains the app and an `Applications` symlink.
  Default input `build/Remote Viewer.app`, output `build/RemoteViewer-<version>.dmg`.
- `docs/macos.md`: "Distribution" section documenting both.

## Acceptance criteria
- [ ] `ruby -c build-aux/macos/virt-viewer.rb` exits 0.
- [ ] `bash build-aux/macos/check.sh && bash build-aux/macos/make-bundle.sh && bash build-aux/macos/make-dmg.sh`
      exits 0; `hdiutil verify build/RemoteViewer-11.0.dmg` exits 0 (paste).
- [ ] `docs/macos.md` "Distribution" section present.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] No files changed outside scope.

## Out of scope
Notarization, code-signing with a Developer ID, changing make-bundle.sh.

## Worker report

> claimed_by: codex-1 (pid 17000) at 2026-08-30T13:54:02Z

Implemented both distribution helpers and completed the PM-requested formula
rework. The DMG acceptance check was executed by the PM on the host because
this worker's sandbox does not permit `hdiutil create`.

Files changed:

- `build-aux/macos/virt-viewer.rb` — added the head-only `VirtViewer` formula,
  using the public HTTPS `mac-port` source, the Homebrew-install dependency
  set, meson/ninja install, and version test.
- `build-aux/macos/make-dmg.sh` — added a cwd-independent compressed-DMG
  helper with bundle-version naming, temporary staging, and Applications link.
- `docs/macos.md` — documented direct/local-tap formula installation and DMG
  creation, defaults, overrides, contents, and signing boundary.

Commands and results:

- `ruby -c build-aux/macos/virt-viewer.rb` — `Syntax OK`, exit 0.
- `bash -n build-aux/macos/make-dmg.sh` and `git diff --check` — exit 0.
- `brew audit --formula build-aux/macos/virt-viewer.rb` — could not reach the
  formula audit because Homebrew attempted a prohibited write under
  `/opt/homebrew/Library/Homebrew/vendor/bundle`; the required `ruby -c`
  fallback passed.
- `bash .tigerteam/scripts/run-tests.sh` — exit 0 after the formula rework.

  ```text
  cmd:  bash build-aux/macos/check.sh
  exit: 0
  log:  .tigerteam/logs/tests/20260830-140131-45403.log (13 lines)
  1/4 virt-viewer:test-version-compare   OK
  2/4 virt-viewer:test-monitor-mapping   OK
  3/4 virt-viewer:test-monitor-alignment OK
  4/4 virt-viewer:test-hotkeys           OK
  Ok:                4
  Fail:              0
  ```

- The initial combined wrapper/bundle/DMG run passed the build, all four tests,
  and bundle creation, then this lane's `hdiutil create` was denied by the
  sandbox. The PM subsequently ran the DMG portion and verification on the
  host, as recorded below.
- `grep -n '^## Distribution$' docs/macos.md` — found line 166.
- `git diff --name-only 8f495e6^..HEAD` — listed only
  `build-aux/macos/make-dmg.sh`, `build-aux/macos/virt-viewer.rb`, and
  `docs/macos.md`.
- `git status --short --branch` after the final commit — clean.

DMG (PM-verified):

```text
$ bash build-aux/macos/make-dmg.sh
created: .../worktrees/T-0008/build/RemoteViewer-11.0.dmg
DMG: .../worktrees/T-0008/build/RemoteViewer-11.0.dmg     (45 MB)
$ hdiutil verify build/RemoteViewer-11.0.dmg
hdiutil: verify: checksum of "build/RemoteViewer-11.0.dmg" is VALID
(exit 0)
```

Documentation: added the required `## Distribution` section with `Homebrew
formula` and `Disk image` subsections.

Commits: `8f495e6` (implementation) and `eac38d2` (HTTPS source and formula
dependency correction) on `tigerteam/T-0008`; final HEAD is `eac38d2`.

Reviewer focus: the DMG commands were verified outside this lane by the PM.
Homebrew's full audit could not run under the sandbox, so the ticket-specified
`ruby -c` fallback was used and passed.

## Questions

The implementation, build, bundle, and test suite succeed, but this worker
cannot execute the DMG acceptance criterion: `hdiutil create` returns
`Operation not permitted`. Retrying with `TMPDIR` inside the worktree produces
the same result, so no DMG exists for `hdiutil verify`.

Options:

1. Re-run `make-dmg.sh` and `hdiutil verify` on an unsandboxed worker with the
   `macbuild` capability (recommended).
2. Accept only static/syntax validation here, leaving creation and verification
   for review.

> handoff: blocked

## Answers

Option 2, with the DMG criterion executed by the PM on the host from your
worktree (14:59):

    $ bash build-aux/macos/make-dmg.sh
    created: .../worktrees/T-0008/build/RemoteViewer-11.0.dmg
    DMG: .../worktrees/T-0008/build/RemoteViewer-11.0.dmg     (45 MB)
    $ hdiutil verify build/RemoteViewer-11.0.dmg
    hdiutil: verify: checksum of "build/RemoteViewer-11.0.dmg" is VALID
    (exit 0)

Two small fixes before handing off, then paste the block above into your
report under "DMG (PM-verified)":
1. `build-aux/macos/virt-viewer.rb`: the `head` URL must be HTTPS —
   `https://github.com/ubyjvovk/virt-viewer.git` — an SSH `git@` URL only
   works for people with push keys.
2. Drop `depends_on "dylibbundler"` from the formula: a Homebrew install
   links against the Cellar and never bundles; dylibbundler is only a
   `make-bundle.sh` tool (leave it in the docs' bundling prerequisites).
`ruby -c` again, `bash .tigerteam/scripts/run-tests.sh` exits 0, commit on
`tigerteam/T-0008`, hand off `review`. Do not retry hdiutil in your lane.

> claimed_by: codex-1 (pid 17000) at 2026-08-30T13:59:53Z
> INCOMPLETE attempt by codex-1 at 2026-08-30T14:00:58Z (engine exit -15, no handoff) — see /Users/d/b/virt-viewer/.tigerteam/logs/workers/codex-1.log

> claimed_by: codex-1 (pid 17000) at 2026-08-30T14:00:58Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 4m 30s | 983.72k (906.24k) | 10.85k | — | — | blocked |
| 2 | codex-1 | gpt-5.6-sol | 1m 5s | — | — | — | — | todo |
| 3 | codex-1 | gpt-5.6-sol | 1m 42s | 229.31k (199.68k) | 4.62k | — | — | review |

total: 3 attempts · 7m 17s · 1.21M in (1.11M cached) / 15.47k out · $0.00 · 1 without usage · 3 without cost (accepted 2026-08-30T14:03:28Z)
