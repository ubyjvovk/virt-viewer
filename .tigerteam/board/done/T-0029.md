---
id: T-0029
title: Native viewer — full keyboard capture via event tap (⌘-Space, ⌘-Tab, system shortcuts)
priority: P1
complexity: C2
depends_on: [T-0025]
scope: [macos-native/]
capability: [macbuild]
---

## Goal
Extend T-0025's keyboard capture to the chords macOS swallows before the
app ever sees them — ⌘-Space, ⌘-Tab, ⌘`, screenshot keys — using a
CGEventTap, so a Hyprland guest bound on SUPER is fully usable. This is
the standard mechanism for this app category (Parallels, VMware Fusion,
Screen Sharing all do it); it requires a one-time Accessibility grant.

## Context
- Read `macos-native/README.md`; T-0025's report in `.tigerteam/board/done/`.
- Locked design (PM):
  - `CGEventTapCreate(kCGSessionEventTap, kCGHeadInsertEventTap,
    kCGEventTapOptionDefault, keyDown|keyUp|flagsChanged mask, …)` on the
    main thread's runloop. In the callback: if capture is ON **and** the
    viewer window is key **and** a session is connected → consume the
    event (return NULL) and forward it through the existing scancode path
    (`CGEventGetIntegerValueField(kCGKeyboardEventKeycode)` is the same
    keycode space as `NSEvent.keyCode`; reuse the keymap + flagsChanged
    synthesis). Otherwise return the event untouched so the rest of the
    system behaves normally. Handle `kCGEventTapDisabledByTimeout` /
    `…ByUserInput` by re-enabling the tap.
  - **Single source rule**: while the tap is installed and enabled, it is
    the ONLY keyboard sender — guard the `VsmView` responder methods
    (keyDown/keyUp/flagsChanged/performKeyEquivalent) with a flag so no
    chord is sent twice. When the tap is unavailable (no Accessibility
    grant), everything falls back to exactly T-0025's behavior.
  - **⌘Q policy (user decision 2026-09-01, mirrors Chrome's hold-to-quit
    + UTM's captured-goes-to-guest):** while capture is active, a TAP of
    ⌘Q is forwarded to the guest like any other chord (Omarchy SUPER+Q =
    close window, used constantly); HOLDING ⌘Q for ~1 s quits the viewer
    — implement with a keyDown-starts / keyUp-cancels timer and show a
    brief "Hold ⌘Q to Quit" overlay (a borderless HUD-style panel or
    NSTextField overlay is fine) while the key is down. On hold-quit,
    send the guest a release for the forwarded press first (no stuck
    Super/Q). When capture is off (or no session / window not key),
    plain ⌘Q quits immediately as today — T-0024's quit monitor keeps
    that role; rework it so both behaviors come from one place instead
    of racing.
  - **Escape chord: ⌃⌥ pressed together then released** (matches the
    T-0027 mouse-ungrab convention) toggles capture OFF from the
    keyboard. It is always handled locally, never forwarded; on toggle,
    release-all-keys to the guest so no modifier sticks. Capture also
    auto-suspends (tap passes everything through) when the window
    resigns key, the app deactivates, or the session disconnects.
  - **Permission flow**: on first enable call
    `AXIsProcessTrustedWithOptions` with the prompt option; if not
    trusted, degrade silently to T-0025 behavior and reflect it in the
    Input menu (e.g. "Capture System Shortcuts (needs Accessibility)"
    disabled-state or suffix), re-checking on each toggle. Never crash,
    never busy-poll TCC.
  - TCC attributes the grant to the responsible process — for a binary
    launched from a shell that is the terminal app, and after T-0026 it
    will be the `.app` (fresh grant then; note this in the README).
- **TCC reality check for your run**: the supervisor's Terminal likely
  has NO Accessibility grant (only Screen Recording). Trigger the prompt
  once, then test `AXIsProcessTrusted()`. If the grant is not present,
  DO NOT wait or retry-loop: implement everything, verify the degraded
  path + pass-through behavior live, verify the capture path as far as
  possible (tap installs, callback sees events, single-source guard) and
  put the exact System Settings steps for the user in your report — the
  PM/user does the final ⌘-Space live check. Do not modify TCC databases
  or use `tccutil`/private APIs.
- Live target `spice://100.101.77.113:5900` (disposable Omarchy VM; user
  allows unlocking + clicking around). Lock-screen password: root `.env`
  `GUEST_UNLOCK_PW` — type it via the viewer to unlock; never echo it
  into reports/logs/commits. No destructive/power chords.
- Engine ceiling ≈45 min: commit early and often; hand off INCOMPLETE
  with precise next steps rather than pushing through.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] `bash macos-native/build.sh` clean under `-Wall -Wextra -Werror`.
- [ ] With Accessibility granted (if obtainable in the run): view
      focused, capture ON → ⌘-Space and ⌘-Tab reach the guest (trace
      scancodes + guest reaction screenshot) and do NOT trigger
      Spotlight/app switcher; with capture OFF or another app focused,
      ⌘-Space/⌘-Tab behave normally (state how verified). If the grant
      is unobtainable, the degraded-path evidence + user instructions
      per Context replace this criterion.
- [ ] No double-send: trace over a typing burst shows exactly one
      press/release pair per keystroke with the tap active.
- [ ] ⌃⌥ escape chord suspends capture, releases all guest keys, and is
      itself never forwarded; re-enabling works.
- [ ] ⌘Q: tap while captured reaches the guest (trace + guest reaction);
      hold ~1 s quits the viewer cleanly (exit 0, all keys released,
      overlay shown while held); uncaptured ⌘Q quits immediately
      including from modal alerts (T-0024's hard-won behavior retained).
- [ ] Auto-suspend on resign-key / deactivate / disconnect (trace or
      log evidence for each).
- [ ] Without the grant the app behaves exactly as T-0025 (no crash, no
      prompt loop; menu reflects the state).
- [ ] `macos-native/README.md` updated (capture tiers, Accessibility
      grant walkthrough, escape chord, re-grant-after-bundling note).
- [ ] Evidence under `macos-native/build/qa/`.
- [ ] No files changed outside scope.

## Out of scope
Private APIs (`CGSSetSymbolicHotKeyEnabled`, symbolic-hotkey push modes),
Input Monitoring-only taps, remapping UI, media/Fn keys, per-chord
passthrough configuration, mouse grabbing (T-0027).

## Worker report

> claimed_by: opus-1 (pid 87894) at 2026-09-01T10:08:29Z

**Status: COMPLETE.** Branch `tigerteam/T-0029`, tip **b340857** (3 commits:
f39a37a, 0ea6818, b340857). The Accessibility grant *was* present on this host
(the user granted it to Terminal during the run), so the full capture path was
verified live against the Omarchy VM — not just the degraded path.

### Files changed

| file | why |
| --- | --- |
| `macos-native/vsm-tap.h` (new) | `VsmEventTap` + `VsmEventTapDelegate`: trust check, install/uninstall, "is it active" |
| `macos-native/vsm-tap.m` (new) | the tap itself: `CGEventTapCreate(kCGSessionEventTap, kCGHeadInsertEventTap, kCGEventTapOptionDefault, keyDown\|keyUp\|flagsChanged)` on the main run loop in `kCFRunLoopCommonModes`, `kCGEventTapDisabledBy{Timeout,UserInput}` re-enable, `CGEvent`→`NSEvent` so the tap path reuses the existing keymap and flags-changed synthesis |
| `macos-native/main-view.h` | `tapOwnsKeyboard` (the single-source flag) + `sendKeyCode:down:` / `handleFlagsChanged:` exposed as the tap's entry points |
| `macos-native/main.m` | responder guards; capture policy (`captureActive`, `setCapture:reason:`, `updateKeyboardOwnership`, `installTapPrompting:`); tap decisions (`tapKeyDown/tapKeyUp/tapFlagsChanged`); ⌃⌥ escape chord; the one ⌘Q decision point `quitActionForKeyDown:` + hold timer + `VsmHUDPanel` overlay; Input-menu tier item; auto-suspend logging |
| `macos-native/vsm-spice.c` | `release_all_keys()` now reports `released N held keys` (only when N>0) — the escape chord's "no stuck modifier" criterion is otherwise unobservable |
| `macos-native/build.sh` | build `vsm-tap.m` |
| `macos-native/README.md` | capture tiers table, Accessibility walkthrough + re-grant-after-bundling note, escape chord, ⌘Q rule table, `VSM_NO_EVENT_TAP`, layout table |

### Commands run

```
bash .tigerteam/scripts/run-tests.sh
  1/5 virt-viewer:test-version-compare     OK   0.03s
  2/5 virt-viewer:test-monitor-mapping     OK   0.03s
  3/5 virt-viewer:test-monitor-alignment   OK   0.13s
  4/5 virt-viewer:test-hotkeys             OK   0.13s
  5/5 virt-viewer:test-macos-window-state  OK  15.88s
  Ok: 5   Fail: 0
bash macos-native/build.sh    -> clean under -Wall -Wextra -Werror
```

### Acceptance criteria

All evidence is in `macos-native/build/qa/` (indexed by `EVIDENCE.md`);
synthetic keystrokes came from `post-keys.m` there, which uses `CGEventPost`,
so the tap sees them exactly as it sees a physical keyboard.

- **run-tests.sh exits 0** — above.
- **build.sh clean** — above.
- **⌘Space / ⌘Tab captured** — `capture.log`: `xt=0x015b` (Super) + `0x0039`
  (Space) and `+0x000f` (Tab), one pair each. Guest reaction: `frame-2.png`
  shows Omarchy's SUPER+Space menu open. Mac at the same moment
  (`mac-cmdspace.png`, `d1-cmdtab.png`, `d2-cmdtab.png`): no Spotlight, no app
  switcher, frontmost still `spice-viewer` (checked via System Events before
  and after). **Capture off**: nothing forwarded and macOS handles the chord —
  frontmost went `spice-viewer` → `Brave Browser` on ⌘Tab.
- **No double-send** — `capture2.log`, burst `h e l l o`: `0x23,0x12,0x26,
  0x26,0x18`, exactly one press and one release each; guest shows one `hello`.
- **⌃⌥ escape chord** — `capture2.log`: ctrl and alt presses forwarded, ctrl
  release forwarded, **the completing alt release never forwarded**, then
  `released 1 held key` + `keyboard capture off (ctrl-alt escape chord)`.
  Re-enabled from the Input menu (`keyboard capture on (menu)`) and capture
  worked again.
- **⌘Q** — tap: `quit.log` `Cmd-Q down while captured: forwarded to the guest,
  hold 1 s to quit`, press+release forwarded, process still alive; guest
  reaction in `frame-3.png`. Hold: `holdquit.log`, at +1.002 s the forwarded
  press is released, `released 1 held key`, `disconnected cleanly`, **exit
  status 0**; `hud-hold.png` shows the overlay while the key is down.
  Uncaptured: `suspend.log` (after the escape chord) exit 0, and `modal.log`
  ⌘Q from the modal disconnect alert quits — T-0024's behaviour retained.
- **Auto-suspend** — `autosuspend.log` logs each trigger separately:
  `viewer window resigned key`, `application deactivated`; `holdquit.log` /
  `modal.log` log `session disconnected`. Nothing is forwarded while
  suspended.
- **No grant → T-0025 exactly** — `degraded.log` (`VSM_NO_EVENT_TAP=1`): menu
  item reads `Capture System Shortcuts (needs Accessibility)` and is
  unchecked, `Capture Keyboard` still ✓; captured ⌘W and ⌘Q reach the guest as
  a same-millisecond press/release pair (the `performKeyEquivalent:`
  signature), no hold-to-quit, no quit; uncaptured ⌘Q quits at once. No crash,
  and the system prompt is asked for at most once per process.
- **README updated** — tiers, grant walkthrough, escape chord, ⌘Q table,
  re-grant-after-bundling note.
- **No files changed outside scope** — all seven files are under
  `macos-native/`.

### Two real bugs found and fixed during live QA (worth the reviewer's eye)

1. **⌘Q was never recognised on a non-Latin keyboard layout.** The host's
   active layout is Russian, and `-charactersIgnoringModifiers` returns `й`
   for the Q key, so the chord test failed. This is a **pre-existing T-0024
   bug** (the quit monitor used the same field) that the tap merely exposed;
   `quitActionForKeyDown:` now matches `-characters`, which is what AppKit
   itself matches key equivalents against. `build/qa/dbg.log`.
2. **Exact-equality on the modifier field is wrong for tapped events.** A
   posted/tapped ⌘Q arrives with `flags=0x20100000` — macOS sets an extra bit
   beyond the documented `NSEventModifierFlag*` — so `flags ==
   NSEventModifierFlagCommand` never matched. Now masked to the four
   modifiers that can be meant (which also fixes ⌘Q with Caps Lock on).

Also fixed mid-QA: the hold-to-quit HUD was an ordinary `NSPanel` and became
key when ordered front, which made the viewer window resign key, auto-suspend
capture and cancel the very hold it was announcing. It is now a `VsmHUDPanel`
with `canBecomeKeyWindow == NO`.

### For the reviewer

- **Interpretation of "the escape chord is never forwarded":** a chord is only
  recognisable on release, so the ⌃ and ⌥ *presses* have already gone to the
  guest by the time it fires. What the implementation guarantees is that the
  completing transition is never forwarded and that firing releases every key
  the guest holds, so the net guest state is empty (`released N held keys`
  proves it). Making the presses themselves invisible would need deferred
  modifier forwarding, which I judged out of proportion to the benefit — flag
  it if you disagree.
- The escape chord turns capture **off** only. Making it a true toggle would
  mean intercepting ⌃⌥ while capture is off, which contradicts the "pass
  everything through when not capturing" rule; re-enabling is the Input menu.
- `vsm-spice.c` is touched for one observability line only (`released N held
  keys`), inside `macos-native/` scope. Say the word if even that is unwanted.
- `VSM_NO_EVENT_TAP=1` is a deliberate new test hook (documented in the README
  env table): TCC cannot be revoked from inside the process, so it is the only
  way to exercise the no-grant path on a granted machine.
- Not touched, as specified: private symbolic-hotkey APIs, Input Monitoring
  taps, media/Fn keys, per-chord passthrough config, mouse grabbing (T-0027).
- Out-of-scope observation, not acted on: while capture is active the tap also
  swallows arrow keys during main-menu tracking, so the Input menu is
  mouse-driven while a session is captured. The README already said the menu
  bar is reachable with the mouse; a future ticket could pass events through
  while a menu is tracking.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-5 | 24m 57s | 10.88M (10.88M) | 93.5k | $9.53 | engine | review |

total: 1 attempt · 24m 57s · 10.88M in (10.88M cached) / 93.5k out · $9.53 (accepted 2026-09-01T10:34:52Z)
