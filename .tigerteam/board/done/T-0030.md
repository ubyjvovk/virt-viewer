---
id: T-0030
title: Native viewer — clipboard: answer unfulfillable guest requests with NONE; never offer empty text
priority: P1
complexity: C2
depends_on: []
scope: [macos-native/]
capability: [macbuild]
---

## Goal
Fix a protocol violation found in PM live testing: when the guest
requests clipboard text we advertised and the pasteboard has no
(non-empty) string, `vsm-clipboard.m` logs "host has none" and returns
WITHOUT answering. The agent protocol expects an explicit empty/NONE
notify; a dangling request can wedge the guest agent's clipboard state
machine (observed: after one unanswered request, the guest ignored all
later grabs). Also stop offering when there is nothing to offer.

## Context
- The defect: `macos-native/vsm-clipboard.m` ~line 172 — the
  `!text.length` branch bare-returns. Reference behavior: spice-gtk's
  `src/spice-gtk-session.c` answers an unfulfillable request with
  `spice_main_channel_clipboard_selection_notify(main, selection,
  VD_AGENT_CLIPBOARD_NONE, NULL, 0)` — verify the exact call/enum in the
  unpacked spice-gtk 0.42 source (path in T-0023's Context, done/).
- Locked fixes (PM):
  1. Add a "no data" answer: extend `vsm-spice.h/.c` with a send op
     (e.g. `vsm_spice_clipboard_send_none()`) following the existing
     GLib-thread marshalling pattern, and call it from the empty branch
     so EVERY guest request gets exactly one answer.
  2. Don't advertise what we don't have: the poller (first poll
     included) must offer only when the pasteboard has a NON-EMPTY
     string. An empty pbcopy item must produce no grab. The
     offer→request→cleared race is then covered by fix 1.
  3. Trace lines for both (lengths only, never contents).
- Live target `spice://100.101.77.113:5900` (disposable Omarchy VM,
  benign input only). The guest's `spice-vdagent` is flaky today (user
  is restarting it repeatedly; it also crashes on data install — a
  separate guest-side bug, out of scope). If the agent is up:
  reproduce the PM's A/B — connect with an EMPTY host pasteboard
  (`pbcopy </dev/null`), see the guest request, verify the NONE answer
  goes out (SPICE_DEBUG/VSM_TRACE), then `pbcopy` a marker and confirm
  the guest still requests the new offer (agent not wedged). If the
  agent is down/crashed, evidence degrades to: trace showing no offer
  on empty pasteboard + code inspection of the NONE path + the
  spice-gtk parallel cited; say so plainly.
- The viewer's pasteboard poller runs only while the app is ACTIVE —
  activate the window (osascript is granted) or the offer never fires.
- Engine ceiling ≈45 min; commit early; INCOMPLETE handoff over
  pushing through.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] `bash macos-native/build.sh` clean under `-Wall -Wextra -Werror`.
- [ ] Empty pasteboard produces NO clipboard grab (trace evidence).
- [ ] A guest request that cannot be fulfilled is answered with the
      NONE notify (trace or SPICE_DEBUG evidence if the agent is up;
      else code path + citation).
- [ ] If the agent is up: after a NONE answer, a subsequent non-empty
      offer still elicits a guest request (not wedged).
- [ ] Normal path regression: non-empty host copy still offered and
      served (trace).
- [ ] `macos-native/README.md` clipboard section updated (one line on
      the NONE answer).
- [ ] Evidence under `macos-native/build/qa/`.
- [ ] No files changed outside scope.

## Out of scope
The guest vdagent's crash-on-install (guest software), guest→host
direction changes, images/rich text, PRIMARY selection.

## Worker report

> claimed_by: opus-1 (pid 87894) at 2026-09-01T15:39:24Z

Branch `tigerteam/T-0030`; commits **26577d9** (code) and **c7dd59a** (docs).
All acceptance criteria met, including the live A/B — the guest agent was up
for the whole run.

### Files changed
- `macos-native/vsm-spice.c` — new `do_clipboard_send_none()` (GLib thread) +
  `vsm_spice_clipboard_send_none()` entry point, marshalled with the existing
  `invoke()`/`input_op()` pattern; sends
  `spice_main_channel_clipboard_selection_notify(main, CLIPBOARD,
  VD_AGENT_CLIPBOARD_NONE, NULL, 0)` and traces `host -> guest, none (0 bytes)`.
- `macos-native/vsm-spice.h` — declares it, with the "every request gets exactly
  one answer" rule on the `clipboard_request` callback and on
  `vsm_spice_clipboard_send()`; "all four" -> "all five".
- `macos-native/vsm-clipboard.m` — (a) the `!text.length` branch in
  `-guestRequestedText` now calls `vsm_spice_clipboard_send_none()` instead of
  bare-returning; (b) `-pollPasteboard` offers only when the pasteboard holds a
  NON-EMPTY string (`stringForType:` length, reduced to a count on the spot)
  rather than when the text flavour merely exists; (c) a third trace branch for
  "text flavour present, nothing behind it".
- `macos-native/vsm-clipboard.h` — `-guestRequestedText` doc updated.
- `macos-native/README.md` — clipboard section: the non-empty-offer rule, the
  NONE answer with the spice-gtk parallel and the wedge it prevents, four -> five
  `vsm_spice_clipboard_*` functions.

### Reference verified (as the ticket asked)
- `VD_AGENT_CLIPBOARD_NONE = 0` — `/opt/homebrew/include/spice-1/spice/vd_agent.h:279`.
- spice-gtk 0.42 `src/spice-gtk-session.c:1399` sets `type =
  VD_AGENT_CLIPBOARD_NONE` and `:1435` passes it to
  `spice_main_channel_clipboard_selection_notify()` for a request it cannot
  fulfil. Note for the reviewer: spice-gtk's *text* path
  (`clipboard_received_text_cb`, `:1061`) answers a failed retrieval with
  `UTF8_TEXT` + `NULL`/0 instead. Both are empty answers that close the request;
  I implemented NONE as the PM locked, and it is the more explicit of the two.
- `agent_clipboard_notify()` (`src/channel-main.c:1424`) accepts `data == NULL,
  size == 0` — it queues the header only — so a NONE notify is a well-formed
  `VD_AGENT_CLIPBOARD` message, not a zero-length write.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh` -> exit 0:
  `Ok: 5 / Fail: 0` (test-version-compare, test-monitor-mapping, test-hotkeys,
  test-monitor-alignment, test-macos-window-state).
- `bash macos-native/build.sh` -> exit 0, no diagnostics under
  `-Wall -Wextra -Werror` (flags at `macos-native/build.sh:19`).
- Live: `VSM_TRACE=1 SPICE_DEBUG=1 macos-native/build/spice-viewer
  spice://100.101.77.113:5900`, one session 16:42–16:45, PID recorded and killed
  by PID.

### Live evidence (agent WAS up; full write-up in `macos-native/build/qa/T-0030-summary.md`)
- **Empty pasteboard -> no grab.** Connected with `pbcopy </dev/null`:
  `16:42:44.622 clipboard: guest agent present, text clipboard active` /
  `16:42:44.623 clipboard: host copy seen (change 2699) with no text, nothing
  offered`. No `offering ...` line, no guest request. (`pbcopy </dev/null` does
  leave the text flavour on the pasteboard, so this is the exact case that used
  to grab.)
- **Normal path regression.** 12-char marker:
  `16:43:19.290 offering 12 characters to guest` ->
  `16:43:19.346 guest requested UTF8 text` ->
  `16:43:19.346 answering guest request, 12 characters` ->
  `16:43:19.346 host -> guest, 12 bytes`. Request latency 56 ms.
- **NONE answer, live.** Forced the offer->request->cleared race by copying
  while the app was inactive (poller off), activating (first poll offers at
  once) and emptying the pasteboard 10 ms later:
  `16:44:07.392 offering 12 characters to guest` ->
  (pasteboard emptied 16:44:07.399) ->
  `16:44:07.402 guest requested UTF8 text` ->
  `16:44:07.404 guest asked for text, host has none, answering none` ->
  `16:44:07.404 host -> guest, none (0 bytes)`.
- **Not wedged.** 35 s after that NONE answer, a new 12-char offer was still
  pulled: `16:44:42.817 offering 12 characters` ->
  `16:44:42.924 guest requested UTF8 text` -> `host -> guest, 12 bytes`.
- **No contents leaked.** `grep -c T30-MARKER macos-native/build/qa/live-a-empty.log`
  -> `0`, with `SPICE_DEBUG=1` on.

### Evidence artifacts (`macos-native/build/qa/`)
- `T-0030-summary.md` — the A/B write-up above with the exact trace excerpts.
- `live-clipboard-trace.txt` — clipboard/agent lines from the session.
- `live-a-empty.log` — the raw 2625-line session log (SPICE_DEBUG + VSM_TRACE).

### For the reviewer
- One offer during the run drew no guest request: `16:44:18.475 offering 23
  characters to guest`, nothing back for 24 s — but the very next offer was
  pulled 107 ms after its grab, so this is the guest manager choosing not to
  pull, not a stuck client. Recorded in the summary rather than smoothed over.
- `-pollPasteboard` now reads the pasteboard string every time the changeCount
  moves, where it used to only test for the flavour. That is the cost of
  telling an empty copy from a real one; the string is reduced to a length
  immediately and never traced or stored. If a reviewer objects to reading a
  large pasteboard once a second, the alternative is offering empties again.
- Out of scope, untouched, noticed while testing: activating the viewer window
  via `osascript` delivers the current mouse position (and any keystroke that
  lands during the switch) straight into the guest — visible in the trace as
  `motion`/`key down` lines right after activation. Worth a look for a future
  ticket about focus-in behaviour.
- The guest's vdagent did not crash at any point during this run, so the
  degraded-evidence path in the ticket was not needed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-5 | 7m 11s | 2.3M (2.3M) | 26.16k | $2.53 | engine | review |

total: 1 attempt · 7m 11s · 2.3M in (2.3M cached) / 26.16k out · $2.53 (accepted 2026-09-01T15:47:51Z)
