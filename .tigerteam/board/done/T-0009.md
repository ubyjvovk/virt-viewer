---
id: T-0009
title: QA — launch Remote Viewer.app on the host, screenshot, report
priority: P1
complexity: C2
capability: [macbuild]
depends_on: [T-0004, T-0005, T-0006, T-0007, T-0010]
scope: [docs/macos.md]
---

## Goal
Prove the packaged app actually runs as a macOS GUI app and record the
evidence. This ticket produces a report and screenshots; the only allowed
code change is a "Known issues" list in `docs/macos.md`.

## Context
- You run UNSANDBOXED with access to WindowServer. Steps:
  1. `bash build-aux/macos/check.sh && bash build-aux/macos/make-bundle.sh`
  2. `mkdir -p build/qa`; `open "build/Remote Viewer.app"`; wait 5 s;
     `screencapture -x build/qa/T-0009-launch.png` — the connect dialog must
     be visible.
  3. Verify the macOS menu bar shows the app's menus (T-0005):
     screenshot `build/qa/T-0009-menubar.png`.
  4. In the connect dialog, enter `spice://127.0.0.1:1` and connect;
     screenshot the resulting error dialog `build/qa/T-0009-error.png`.
  5. Quit via ⌘Q; confirm the process exited (`pgrep -f 'Remote Viewer' ||
     echo gone`).
  6. Run the bundle on a "clean" environment:
     `env -i PATH=/usr/bin:/bin HOME=$HOME "build/Remote Viewer.app/Contents/MacOS/remote-viewer-launcher" --version`
     (or the actual CFBundleExecutable) prints the version — proves no
     Homebrew env leaks.
  7. `log show --last 2m --predicate 'process == "remote-viewer"' | grep -iE 'error|warn' | head -20`
     → summarize anything alarming.
- Write findings to `build/qa/T-0009-report.txt` AND into the Worker report.
  Every defect found becomes a bullet in `docs/macos.md` "Known issues";
  do not fix anything else here.
- Always `pkill -f 'Remote Viewer.app'` before finishing.

## Acceptance criteria
- [ ] The three screenshots + report exist under `build/qa/` and are named
      in the Worker report with one sentence each describing what is visible.
- [ ] Step 6 output pasted in the report.
- [ ] `docs/macos.md` "Known issues" section exists (may say "none found").
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] No files changed outside scope.

## Out of scope
Fixing anything. File follow-up findings in the report; the PM will ticket them.

## Worker report

> claimed_by: opus-1 (pid 17000) at 2026-08-30T14:22:32Z
> RETRY: killed by operator at 2026-08-30T14:24:47Z (no attempt penalty)

> claimed_by: opus-1 (pid 17000) at 2026-08-30T14:26:04Z

### Verdict

The bundle builds, launches, draws its window and installs a real macOS menu
bar — but it **aborts (SIGABRT) on the first themed icon**, which is hit as
soon as you press *Connect*, and **⌘Q cannot quit it**. Nothing was fixed
(out of scope); every defect is a bullet in `docs/macos.md` "Known issues" and
is written up with repro commands in `build/qa/T-0009-report.txt`.

### Files changed

- `docs/macos.md` — appended a `## Known issues` section (7 defects, the two
  fatal ones with root cause). Only file touched; everything else is under
  `build/`, which is gitignored.

### Screenshots (all under `build/qa/`)

- `T-0009-launch.png` — full second display right after
  `open "build/Remote Viewer.app"`: the "Connection details" window with the
  address entry, the `spice://foo.example.org:5900` hint, the empty "Recent
  connections" list and Cancel/Connect, and the macOS menu bar reading
  "Remote Viewer  Machine  Send key  More actions".
- `T-0009-menubar.png` — the menu bar strip with the *Machine* menu pulled
  down showing Pause / Reset / Power down, all insensitive (no guest).
- `T-0009-error.png` — what actually happens on *Connect*: macOS's
  "Remote Viewer quit unexpectedly." alert with Reopen / Ignore / Report,
  i.e. the app crashed instead of showing an error dialog.
- `T-0009-error-recovered.png` — bonus: the intended
  "Unable to connect to the graphic server spice://127.0.0.1:1" [OK] dialog
  over the main "Remote Viewer" window ("Connecting to graphic server"),
  obtained only after working around the loader defects **outside the repo**
  (in `/tmp`). Proof that the connect path itself is sound.
- `T-0009-report.txt` — the full report; `T-0009-connect-stderr.log` /
  `-stdout.log` — output of the crashing run.

### Commands run

```
bash .tigerteam/scripts/run-tests.sh          -> exit 0
  1/4 virt-viewer:test-monitor-mapping   OK              0.03s
  2/4 virt-viewer:test-version-compare   OK              0.04s
  3/4 virt-viewer:test-monitor-alignment OK              0.11s
  4/4 virt-viewer:test-hotkeys           OK              0.11s
  Ok: 4   Fail: 0
bash build-aux/macos/make-bundle.sh           -> exit 0 ("remote-viewer version 11.0", 108M)
```

Step 6 (clean environment), verbatim:

```
$ env -i PATH=/usr/bin:/bin HOME=$HOME \
    "build/Remote Viewer.app/Contents/MacOS/remote-viewer-launcher" --version
remote-viewer version 11.0
exit=0
```

Step 7 (`log show --last 5m --predicate 'process == "remote-viewer"'`): 722
lines, ~20 error/warn, all Apple framework boilerplate — AppKit
StateRestoration `Unable to find className=(null)`, BaseBoard `task name port
right for pid 463`, CoreSpotlight `MailCS ... skip:1`, TextInputUI
`NSViewBridgeErrorCanceled`. Nothing attributable to the app.

Step 5 (⌘Q): the app did **not** exit. Final state is clean —
`pkill -f 'Contents/MacOS/remote-viewer'` then `pgrep -f 'Remote Viewer'` →
`gone`.

### Findings, severity order

1. **P1 — module caches use CWD-relative paths.**
   `Contents/Resources/lib/gdk-pixbuf-2.0/2.10.0/loaders.cache` and
   `.../lib/gtk-3.0/3.0.0/immodules.cache` list `"loaders/libpixbufloader_svg.so"`
   / `"immodules/im-quartz.so"`. gdk-pixbuf and GTK resolve those against the
   process CWD, not the cache directory, so no module ever loads. Created on
   purpose by `relativize_cache` at `build-aux/macos/make-bundle.sh:160-174`.
   `GDK_PIXBUF_MODULEDIR` does not fix it (verified) — it only affects cache
   generation.
2. **P1 — duplicate `LC_RPATH` on `libpixbufloader_svg.so`.**
   `otool -l ".../loaders/libpixbufloader_svg.so" | grep -A2 LC_RPATH` lists
   `@executable_path/../Resources/lib/` twice and dyld refuses the load with
   `duplicate LC_RPATH`, so even an absolute cache path fails. It is the only
   affected module (all 10 pixbuf loaders + 9 immodules were checked).
   (1)+(2) together are what crash the app: GTK uses `g_assert()` in
   `ensure_surface_for_gicon()` (`gtk/gtkiconhelper.c:495`), so a failed icon
   load aborts the process. Faulting stack, identical in all three crash
   reports `~/Library/Logs/DiagnosticReports/remote-viewer-2026-08-30-{153037,153130,153249}.ips`:
   `abort < g_assertion_message_error < ensure_surface_for_gicon <
   gtk_icon_helper_load_surface < _gtk_icon_helper_draw < gtk_entry_draw`.
3. **P1 — ⌘Q and the Quit menu items are dead.** With the app verified
   frontmost, ⌘Q does nothing; the menu item is disabled
   (`AXEnabled` of *Quit Remote Viewer* and *Quit and Close All Windows* =
   `false`). Escape and the window close button do not terminate it either, so
   there is no GUI route out of the app.
4. **P2 — no native input method**: `im-quartz.so` never loads (consequence of
   finding 1); stderr shows `Loading IM context type 'quartz' failed` on every
   launch.
5. **P3 — Homebrew path leak**: `immodules.cache` entries still carry
   `/opt/homebrew/Cellar/gtk+3/3.24.52/share/locale`.
6. **P3 — non-native window chrome**: the main viewer window keeps the GTK
   client-side header bar with its own minimise/maximise/close buttons and no
   macOS traffic lights (see `T-0009-error-recovered.png`).
7. **P3 — no accessibility tree for GTK content**: only the title bar buttons
   are exposed to the macOS AX API, so VoiceOver and AX automation cannot
   reach dialog contents.

### For the reviewer

- Findings 1 and 2 contradict the STATE note that T-0006 "fixed T-0004's
  bundle (launcher writes absolute module caches; LC_RPATH dedupe)" — on this
  branch the caches are still relative and the SVG loader still has a doubled
  rpath. Worth a targeted follow-up ticket on `build-aux/macos/make-bundle.sh`.
- Proven fix direction, run entirely in `/tmp` (nothing in the repo touched):
  copy the loaders dir aside, `install_name_tool -delete_rpath
  '@executable_path/../Resources/lib/'` the SVG loader, re-ad-hoc-sign it,
  rewrite the cache with absolute paths and point `GDK_PIXBUF_MODULE_FILE` at
  it — the identical connect attempt then survives with clean stderr and shows
  the real error dialog.
- Minor, unexplained: the very first `open "build/Remote Viewer.app"` left the
  window on a non-active Space and the app did not come to the front; later
  launches were fine. Recorded but not diagnosed.
- The QA screenshots are full-display captures of the operator's desktop, so
  they contain unrelated windows around the app. They stay under `build/`
  (gitignored).
- Commit `01aca71` on branch `tigerteam/T-0009`, `docs/macos.md` only.
- Tests were green before the change as well (first `run-tests.sh` of this
  attempt, before any edit: 4/4 Ok).

> handoff: review

## Rework (attempt 2)

Your attempt ran on a tree from 15:26 that did not include T-0006 (merged
15:29), which fixed exactly your findings 1, 2, 4 and 5 (absolute caches
written by the launcher, LC_RPATH dedupe, per-module signing). Finding 3
(⌘Q / Quit disabled) is real and now has its own fix ticket T-0010; this
ticket now depends on it.

When you resume: (1) `git merge --no-edit mac-port` into your branch
`tigerteam/T-0009` (fast-forward is fine) so you QA the current tree —
confirm `git log --oneline -1 mac-port` is an ancestor; (2) rebuild
(`check.sh` + `make-bundle.sh`), rerun every step, retake all screenshots;
(3) rewrite the "Known issues" section from scratch to match what you observe
now — do not carry over items that no longer reproduce; keep the P3 items
(window chrome, AX tree) if still true; (4) re-verify ⌘Q since T-0010 will
have landed; (5) kill only exact PIDs — never `pkill -f`. Same acceptance
criteria as before.


> claimed_by: opus-1 (pid 17000) at 2026-08-30T15:08:26Z

### Verdict (attempt 2 — after merging `mac-port` @ ac57dca)

**PASS.** On the current tree the bundle launches as a real macOS GUI app,
installs a native menu bar, performs a connection attempt, shows its own
"Unable to connect to the graphic server" dialog instead of crashing, and
quits with ⌘Q. Both P1 defects from attempt 1 are gone; four cosmetic /
environmental issues remain and are the new `docs/macos.md` "Known issues"
list. Nothing was fixed here — QA only.

Re-tested every attempt-1 finding:

| attempt-1 finding | now |
|---|---|
| 1 — module caches use CWD-relative paths | **fixed** (launcher writes absolute caches to `~/Library/Caches/org.virt-manager.remote-viewer/`) |
| 2 — duplicate `LC_RPATH` on `libpixbufloader_svg.so` | **fixed** (scan of every bundled `.so`/`.dylib`: no duplicates) |
| 3 — ⌘Q / Quit menu items dead | **fixed** (T-0010) |
| 4 — no native input method | **fixed** (clean-env GUI run: zero stderr) |
| 5 — Homebrew paths in `immodules.cache` | still present (P3) |
| 6 — non-native window chrome on the main window | still present (P3) |
| 7 — no accessibility tree for GTK content | still present (P3) |

### Files changed

- `docs/macos.md` — "Known issues" section rewritten from scratch: the two
  fatal items and the two module-loading items are gone (they no longer
  reproduce), the three surviving P3s are restated against today's evidence,
  and two new items were added (error-dialog placement, first-launch
  activation). Only file touched; everything else is under gitignored `build/`.

### Screenshots and artifacts (all under `build/qa/`)

- `T-0009-launch.png` — the "Connection details" window right after
  `open "build/Remote Viewer.app"`: native macOS title bar with real traffic
  lights, empty "Connection Address" entry, the
  `spice://foo.example.org:5900` hint, empty "Recent connections" list,
  Cancel and an insensitive Connect.
- `T-0009-menubar.png` — the system menu bar reading
  "Remote Viewer | Machine | Send key | More actions" with the *Machine* menu
  pulled down (Pause / Reset / Power down, all insensitive — no guest).
- `T-0009-error.png` — the result of pressing Connect: the main "Remote
  Viewer" window showing "Connecting to graphic server" behind the modal
  "Unable to connect to the graphic server spice://127.0.0.1:1" [OK] dialog.
  No crash. Also the evidence for findings 2 and 4 below (GTK header bar on
  the main window; the dialog sitting in the screen's top-left corner).
- `T-0009-connect-entry.png` — the entry holding `spice://127.0.0.1:1` just
  before Connect.
- `T-0009-report.txt` — the full report. `T-0009-logshow.txt` — step 7 raw
  capture. `T-0009-cleanenv-stderr.log` / `-stdout.log` — both **empty**,
  from the clean-environment GUI run.

The screenshots are region captures around the app's own windows, not
full-desktop grabs, so they no longer carry the operator's unrelated windows.

### Commands run

```
git merge --no-edit mac-port                  -> fast-forward-equivalent merge,
                                                 mac-port (ac57dca) is an ancestor of HEAD
bash .tigerteam/scripts/run-tests.sh          -> exit 0   (run twice: after the merge, and after the docs edit)
  1/4 virt-viewer:test-version-compare   OK              0.04s
  2/4 virt-viewer:test-monitor-mapping   OK              0.03s
  3/4 virt-viewer:test-monitor-alignment OK              0.12s
  4/4 virt-viewer:test-hotkeys           OK              0.13s
  Ok: 4   Fail: 0
bash build-aux/macos/make-bundle.sh           -> exit 0 ("remote-viewer version 11.0", 108M)
codesign --verify --deep --strict "build/Remote Viewer.app"  -> exit 0 (Signature=adhoc)
spctl --assess --type execute -vv ...                        -> rejected (expected for ad-hoc)
```

Step 6 (clean environment), verbatim:

```
$ env -i PATH=/usr/bin:/bin HOME=$HOME \
    "build/Remote Viewer.app/Contents/MacOS/remote-viewer-launcher" --version
remote-viewer version 11.0
exit=0
```

The same clean-environment invocation was also run **without** `--version`, as
a GUI process: the "Connection details" window came up and the process wrote
**zero** lines to stderr. Attempt 1 got `Loading IM context type 'quartz'
failed` on every launch, so the input-module path is genuinely fixed.

Step 5 (⌘Q): ⌘Q was sent twice — once with the connect dialog up after the
failed connection, once to a freshly launched instance. Both processes exited
within ~3 s (`pgrep -f 'Remote Viewer'` → no output; `ps -p 30567` → exited).
The application menu reports `Quit Remote Viewer enabled=true` and
`Quit and Close All Windows enabled=true`.

Step 7 (`log show --start '2026-08-30 16:08:00' --predicate 'process ==
"remote-viewer"'`): 929 lines over this attempt's four runs, 26 matching
`error|warn`, **all** Apple framework boilerplate — AppKit StateRestoration
`Unable to find className=(null)`, AppKit NSCGS `<private>`, BaseBoard `task
name port right for pid 463`, BoardServices XPC `Alloc`, CoreSpotlight `MailCS
… skip:1`, QuartzCore `cannot add handler to 2 from 2`, TextInputUI
`NSViewBridgeErrorCanceled` (self-described as benign). No GTK, GLib or
gdk-pixbuf warnings at all, and no new crash report: nothing matching
`remote-viewer*` in `~/Library/Logs/DiagnosticReports` is newer than the 16:05
rebuild (the five files from 15:30–15:36 are attempt 1's).

### Findings, severity order — all P3, none blocking

1. **Homebrew paths leak into `immodules.cache`.** Both the bundled cache and
   the launcher's absolute-path copy still name
   `/opt/homebrew/Cellar/gtk+3/3.24.52/share/locale` as each input module's
   translation directory. The modules load; only their displayed names lose
   translation on a machine without Homebrew.
   `grep -c homebrew "build/Remote Viewer.app/Contents/Resources/lib/gtk-3.0/3.0.0/immodules.cache"`
   → 11.
2. **Main viewer window is not visually native** — keeps the GTK client-side
   header bar (hamburger, minimise, maximise, close) instead of traffic
   lights, while the connect and error dialogs *do* get native title bars, so
   the inconsistency is within one app. `T-0009-error.png`.
3. **GTK content is invisible to the accessibility API.** The AX tree of the
   connect dialog holds only `close button`, `full screen button`,
   `minimize button`, `group` and the title static text; the entry, the list
   and the buttons are absent and `AXFocusedWindow` is unset. VoiceOver cannot
   read the dialog and AX automation cannot drive the app — this QA had to
   fall back to synthetic keystrokes and screenshots.
   Repro: `osascript -e 'tell application "System Events" to tell (first
   application process whose unix id is <pid>) to tell window "Connection
   details" to get entire contents'`
4. **The connection-error dialog is placed in the screen corner.** Its AX
   position was `{0, 35}` while the parent window was `{0, 12}` sized
   `{1076, 867}` — it opens flush at the top-left of the display, under the
   menu bar, instead of centred on its parent.
5. **A freshly built bundle may not activate on its first launch.** The first
   `open "build/Remote Viewer.app"` after `make-bundle.sh` left the window on
   the secondary display without bringing the app to the front; the two later
   launches activated normally. Attempt 1 saw the same thing on its first
   launch. Not diagnosed.

### For the reviewer

- The two P1 defects attempt 1 reported are genuinely gone, not worked around:
  the connect path was exercised end-to-end on the *unmodified* bundle in the
  repo (attempt 1 could only reach the error dialog after patching a copy in
  `/tmp`).
- The caches shipped *inside* the bundle are still relative. That is by design
  now — `Contents/MacOS/remote-viewer-launcher` rewrites both into
  `~/Library/Caches/org.virt-manager.remote-viewer/` with absolute paths on
  every launch and exports `GDK_PIXBUF_MODULE_FILE` / `GTK_IM_MODULE_FILE`.
  Worth knowing that the bundle therefore depends on a writable cache
  directory; the launcher does degrade gracefully if it is not (comment at
  `build-aux/macos/make-bundle.sh`, launcher heredoc).
- `spctl --assess` rejects the ad-hoc-signed bundle. Expected, and already
  covered by the "Signing" section, but it is what a user downloading the
  T-0008 DMG will hit, so I listed it in "Known issues" explicitly marked as
  expected with a link back to that section. Drop the bullet if you consider
  it duplication.
- Finding 3 (no AX tree) is the one with real user impact of the four:
  it makes the app unusable with VoiceOver. It is a gtk-mac-integration /
  GTK-on-quartz limitation rather than something this port introduced, so I
  did not raise its severity, but it may deserve its own ticket.
- Nothing outside `docs/macos.md` changed (`git status --short` → ` M
  docs/macos.md` before the commit). Commit `c8e229b` on branch
  `tigerteam/T-0009`; the merge commit before it is `0ae4ce4`.
- Tests were green before the docs edit as well (first `run-tests.sh` of this
  attempt, immediately after the merge: 4/4 Ok).
- Cleanup: no remote-viewer process is left running (`pgrep -f 'Remote Viewer'`
  → no output). Only exact PIDs were signalled; `pkill -f` was not used, per
  the rework note.
- Operator-visible side effect worth knowing for future GUI QA tickets: the
  app has to be frontmost to receive synthetic keystrokes, so while step 4 ran
  it captured a few characters the operator was typing into another
  application. They were cleared before the screenshots were taken (visible
  only in the discarded first capture), but a GUI QA pass cannot be run
  invisibly on a machine someone is using.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | opus | 2m 15s | — | — | — | — | todo |
| 2 | opus-1 | claude-opus-5 | 15m 5s | 5.29M (5.29M) | 45.64k | $4.80 | engine | review |
| 3 | opus-1 | claude-opus-5 | 9m 35s | 2.82M (2.82M) | 34.03k | $3.10 | engine | review |

total: 3 attempts · 26m 56s · 8.1M in (8.1M cached) / 79.67k out · $7.90 · 1 without usage · 1 without cost (accepted 2026-08-30T15:19:53Z)
