---
id: T-0027
title: Native viewer — relative (server) mouse mode with grab/ungrab
priority: P2
complexity: C2
depends_on: [T-0026]
scope: [macos-native/]
capability: [macbuild]
---

## Goal
Support SPICE server (relative) mouse mode: when the guest switches to
relative mode, grab the pointer, feed motion deltas via
`spice_inputs_channel_motion()`, and release the grab with a hotkey.
Today relative mode is logged and ignored (T-0022 gap list, ~250 LoC).

## Context
- Read `macos-native/README.md`; prior reports in `.tigerteam/board/done/`.
- API reference: spice-gtk 0.42 source (path in T-0023's Context) —
  `SpiceMainChannel` `main-mouse-update` signal / `mouse-mode` property;
  `spice-widget.c` implements exactly this grab dance for GTK; check
  whether the client can REQUEST a mode
  (`spice_main_channel_request_mouse_mode` or its deprecated alias in
  0.42) — you will need it for testing.
- Locked decisions (PM):
  - Mouse-mode plumbing: new callback `mouse_mode(void *user, int
    relative)` on `VsmSpiceCallbacks` (main-thread, like the rest); new
    `void vsm_spice_send_motion(VsmSpice *self, int dx, int dy, int
    button_state)` for deltas.
  - Grab = clicking the view while in relative mode (and automatically on
    mode switch if the window is key):
    `CGAssociateMouseAndMouseCursorPosition(false)`, hide the local
    cursor over the view (transparent-cursor mechanism from T-0023),
    use `NSEvent.deltaX/.deltaY` from mouse-moved/dragged events,
    periodically warp the hardware pointer back to the view centre
    (`CGWarpMouseCursorPosition`) so it never hits the screen edge.
  - Ungrab hotkey: **⌃⌥ (Control+Option) pressed together and released** —
    same as virt-viewer's default. Also ungrab on window resign-key and
    app deactivate. While grabbed, append " (press ⌃⌥ to release)" to
    the window title (v-v convention); restore the title on ungrab.
    The ⌃⌥ chord used for ungrab must NOT leak a stuck modifier to the
    guest (release-all or suppress those synthetic modifier events).
  - Re-associate the cursor (`CGAssociateMouseAndMouseCursorPosition(true)`)
    on ungrab, disconnect, quit, and every error path — a wedged pointer
    on the user's machine is the failure mode to fear most; structure the
    code so there is ONE ungrab function and every exit calls it.
  - In absolute mode nothing changes.
- Testing against `spice://100.101.77.113:5900` (disposable, benign input
  only): the guest normally runs absolute (tablet) mode. Use the client
  mode request found above to force server mode for the test run
  (a debug env var like `VSM_FORCE_RELATIVE=1` MAY be committed if inert
  by default and documented). Evidence: trace shows `motion dx,dy`
  streams while grabbed; guest cursor visibly moves (screenshot before /
  after a scripted or manual drag); ungrab restores the local pointer.
- Engine ceiling ≈45 min: commit early and often; hand off INCOMPLETE
  with precise next steps. If the mode request API turns out not to
  exist in 0.42, STOP and block with `## Questions` (do not build a
  server-side workaround).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] `bash macos-native/build.sh` clean under `-Wall -Wextra -Werror`.
- [ ] Live target in forced relative mode: grab on click, trace shows
      delta motion reaching the inputs channel, guest cursor moves
      (screenshots), buttons/scroll still work while grabbed.
- [ ] ⌃⌥ ungrabs: local pointer visible and free, title restored, no
      stuck modifiers on the guest (evidence: trace shows the modifier
      releases, and typing in the guest afterwards behaves normally).
- [ ] Ungrab also on window resign-key and on quit; pointer association
      restored on every path (enumerate them in the report).
- [ ] Mode switch back to absolute (unset the force/request client mode)
      returns to the existing absolute behavior in the same session.
- [ ] `macos-native/README.md` updated (relative mode, grab/ungrab UX,
      the force-relative debug var).
- [ ] Evidence under `macos-native/build/qa/`.
- [ ] No files changed outside scope.

## Out of scope
Cursor-shape work beyond reusing T-0023's hide mechanism, pointer
acceleration tuning, multi-display coordinates, clipboard.

## Worker report

> claimed_by: opus-1 (pid 87894) at 2026-09-01T10:55:42Z

### Outcome
Done. Relative (server) mouse mode is implemented end to end and verified
live against `spice://100.101.77.113:5900` (guest `omarchy`, 2560x1440).
Branch `tigerteam/T-0027`, commit **5b56b0f**; WIP commits `340179f`, `7ede5d5`.

### Files changed
- `macos-native/vsm-spice.h` — new `mouse_mode(void *user, int relative)`
  callback, `vsm_spice_send_motion()`, `vsm_spice_request_mouse_mode()`.
- `macos-native/vsm-spice.c` — `notify::mouse-mode` now reports the mode to
  the main thread instead of logging "ignored"; `do_motion` wraps
  `spice_inputs_channel_motion()`; `do_request_mouse_mode` wraps
  `spice_main_channel_request_mouse_mode()` (present in 0.42, non-deprecated);
  `VSM_FORCE_RELATIVE=1` requests server mode once, from the mode
  notification; trace-only `cursor-move` handler.
- `macos-native/main-view.h` — `VsmViewOwner` protocol (title updates),
  `setRelativeMouseMode:`, `pointerGrabbed`, `grabPointer`,
  `ungrabPointer:`, `noteEscapeChord:`, `resetEscapeChord`.
- `macos-native/main.m` — the grab state machine in `VsmView`
  (grab/ungrab/held-button release/delta accumulation/warp-recentre/⌃⌥
  recogniser), motion dispatch split absolute vs relative, click-to-grab,
  blank cursor while grabbed; in the delegate: base-title + grab suffix,
  `cb_mouse_mode`, ungrab on resign-key / deactivate / teardown, the ⌃⌥
  recogniser moved out of the delegate into the view, and the two
  `VSM_FORCE_RELATIVE`-gated Input menu items.
- `macos-native/vsm-debug.{h,m}` — `VSM_GRAB_SELFTEST=1` script.
- `macos-native/README.md` — new "Relative (server) mouse mode" section, two
  new env vars, scope line, file table.

### Commands run
```
bash macos-native/build.sh          # clean, -Wall -Wextra -Werror
bash .tigerteam/scripts/run-tests.sh
  1/5 virt-viewer:test-version-compare    OK   0.03s
  2/5 virt-viewer:test-monitor-mapping    OK   0.03s
  3/5 virt-viewer:test-monitor-alignment  OK   0.12s
  4/5 virt-viewer:test-hotkeys            OK   0.12s
  5/5 virt-viewer:test-macos-window-state OK  15.99s
  Ok: 5   Fail: 0                        # exit 0
```
Live: `VSM_TRACE=1 VSM_FORCE_RELATIVE=1 VSM_GRAB_SELFTEST=1
VSM_DUMP_DIR=macos-native/build/qa ./macos-native/build/spice-viewer
spice://100.101.77.113:5900`

### Acceptance criteria
- **run-tests.sh exits 0** — yes, above.
- **build.sh clean under -Werror** — yes.
- **Grab on click, deltas reach the inputs channel, guest cursor moves,
  buttons/scroll work while grabbed** — yes. `qa/evidence.txt`:
  419 `motion dx,dy` sends; during the motion run the guest emits a trail of
  cursor-sized damage rects marching in lockstep with the deltas
  (`motion dx,dy 24,16` → `damage 1664,906 64x23` → … → `damage 2336,1425
  32x15`), i.e. the guest pointer travelled from near the top-left to the
  bottom-right corner of its 2560x1440 screen and clamped there.
  `qa/grab-before.png` vs `qa/grab-after.png` differ at exactly the pointer's
  old location (`bbox (1022,377,1038,400)`) — crops in
  `qa/cursor-before-topleft.png` / `qa/cursor-after-moved.png`. Buttons and
  scroll while grabbed: `button 1 press state=0x1` / `release state=0x0` /
  `scroll 3 steps`.
- **⌃⌥ ungrabs; pointer free, title restored, no stuck modifiers** — yes,
  on BOTH keyboard entry points in the same run:
  `ctrl-opt release chord (event tap path)` → `pointer released (ctrl-alt
  escape chord)` → `released 1 held key` → `after chord grabbed=0
  title=omarchy - spice://100.101.77.113:5900` (suffix gone), then a re-grab
  and `re-grab, then the chord again (responder path)` → same result. Exactly
  one key is left to release because the first of the two modifier key-ups is
  forwarded normally and only the second completes the chord.
- **Ungrab on resign-key and quit; every path enumerated** — the single
  function is `-[VsmView ungrabPointer:]`, which calls
  `CGAssociateMouseAndMouseCursorPosition(true)` first and unconditionally.
  Callers: (1) `noteEscapeChord:` ⌃⌥ — both entry points; (2)
  `windowDidResignKey:`; (3) `applicationDidResignActive:`; (4)
  `setRelativeMouseMode:NO` (guest switched to absolute); (5)
  `teardownSession` — which covers disconnect, `windowWillClose:`,
  reconnect/auth retry and `applicationWillTerminate:` (⌘Q, hold-to-quit,
  menu Quit). Backstop: the association is process-scoped, so a kill restores
  it. Observed in the log: `pointer released (guest switched to absolute mouse
  mode)` and `pointer released (ctrl-alt escape chord)`.
- **Switch back to absolute in the same session** — yes. The self-test's last
  step calls `vsm_spice_request_mouse_mode(spice, 0)`; log shows `mouse mode:
  client (absolute)` → `mouse mode: absolute (client)` → `grabbed=0` and the
  next move traces as an absolute `motion 853,960 buttons=0x0`, not
  `motion dx,dy`. `VSM_FORCE_RELATIVE` asks only once, so it does not fight
  the switch back.
- **README updated** — yes: a full "Relative (server) mouse mode" section
  (grab/ungrab UX, the single-ungrab rule, the chord, `VSM_FORCE_RELATIVE`,
  `VSM_GRAB_SELFTEST`, the known gap, the spice-gtk motion-pacing gotcha).
- **Evidence under `macos-native/build/qa/`** — `evidence.txt` (curated),
  `relative-run.log` (full trace), `grab-before.png`, `grab-after.png`,
  `cursor-before-topleft.png`, `cursor-after-moved.png`.
- **No files changed outside scope** — `git status` clean; the commit touches
  only `macos-native/`.

### For the reviewer
- **Known gap, deliberately not fixed (out of scope):** the client does not
  *render* the guest's pointer position. In server mode the server owns the
  position and reports it with the cursor channel's `cursor-move`; I added a
  trace-only handler for it so the gap is visible, but nothing draws it. On a
  guest that composites its pointer into the framebuffer (the test guest does)
  this is invisible; on one that uses the cursor plane the user would see no
  pointer at all while grabbed. Worth a follow-up ticket. Note the test guest
  emitted **zero** `cursor-move` signals, so it is the framebuffer-compositing
  kind.
- **One behaviour change outside the strict letter of the ticket, on purpose:**
  the ⌃⌥ recogniser moved from `VsmAppDelegate.escapeArmed` into
  `-[VsmView noteEscapeChord:]`, and the delegate property is gone. The chord
  now has to fire on the non-tap path too (that is where the ungrab must work
  without an Accessibility grant), and two copies of the recogniser fed by
  disjoint event streams would have been a bug farm. The tap path's observable
  behaviour is unchanged: it still ends with `setCapture:NO reason:@"ctrl-alt
  escape chord"`.
- **`-[VsmView sendButton:]` now ignores a release for a button that is not
  held.** Needed because the grab-taking click is consumed (its `mouseUp`
  would otherwise be a phantom release) and because `ungrabPointer:` releases
  held buttons itself. Absolute mode is unaffected — the mask is always set by
  the matching press there.
- `spice_main_channel_request_mouse_mode()` must not be called at
  `channel-new`; there is no open channel to send it down and the session
  stalls silently (I hit this — first live run hung right after
  `channel-new: SpiceMainChannel id 0`). It is called from the mouse-mode
  notification instead.
- `VSM_GRAB_SELFTEST` briefly grabs the *real* pointer on the machine that
  runs it. It always ends with an un-grab, and the association is restored on
  process exit either way, but do not run it on a machine you are using.
- Not verified: multi-display coordinate handling for the warp-to-centre
  (single-display machine here); `recentrePointerIfDrifted` never actually
  fired in testing, because a disassociated pointer does not move — it is a
  backstop for the window where the OS re-associates it behind our back.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-5 | 21m 11s | 8.41M (8.41M) | 79.49k | $7.80 | engine | review |

total: 1 attempt · 21m 11s · 8.41M in (8.41M cached) / 79.49k out · $7.80 (accepted 2026-09-01T11:18:15Z)
