---
id: T-0004
title: make-bundle.sh — relocatable Remote Viewer.app
priority: P1
complexity: C3
capability: [frontier]
depends_on: [T-0002]
scope: [build-aux/macos/, docs/macos.md, .gitignore]
---

## Goal
A script `build-aux/macos/make-bundle.sh` that turns a finished meson build
into a self-contained, relocatable, ad-hoc-signed `Remote Viewer.app` that
runs on a Mac without Homebrew installed.

## Context
- Inputs: an existing build dir (default `build`, override `BUILD_DIR`),
  output `$BUILD_DIR/Remote Viewer.app` (override `APP_DIR`).
- Approach (PM decision — follow it):
  1. `DESTDIR=<tmp> meson install -C $BUILD_DIR` (prefix is whatever meson
     was configured with; read it via `meson introspect --buildoptions`).
  2. Layout: `Contents/MacOS/remote-viewer` (the real binary, plus
     `virt-viewer` next to it), `Contents/Resources/share/{locale,icons/hicolor,
     mime}` copied from the DESTDIR install, `Contents/Resources/lib/` for
     dylibs, `Contents/Info.plist`, `Contents/Resources/remote-viewer.icns`,
     `Contents/PkgInfo` (`APPL????`).
  3. Runtime data GTK needs that meson does not install — copy from
     `$(brew --prefix)`: `share/glib-2.0/schemas` (then run
     `glib-compile-schemas` on the copy), `share/icons/Adwaita` (only the
     `16x16 22x22 24x24 32x32 48x48 scalable symbolic` subtrees + index.theme
     — keep the bundle < 150 MB), `lib/gdk-pixbuf-2.0/2.10.0/loaders/*.so`
     + regenerate `loaders.cache` with `gdk-pixbuf-query-loaders` into
     `Contents/Resources/lib/gdk-pixbuf-2.0/2.10.0/loaders.cache` using
     bundle-relative paths, `lib/gtk-3.0/3.0.0/immodules` (+ `immodules.cache`),
     `share/mime` (run `update-mime-database` on the copy), GStreamer plugins
     are NOT bundled (audio is out of scope; note it in the doc).
  4. `dylibbundler -od -b -x Contents/MacOS/remote-viewer -x Contents/MacOS/virt-viewer
     -d Contents/Resources/lib/ -p @executable_path/../Resources/lib/`
     then run dylibbundler again over every bundled `.so` loader so their deps
     are also inside the bundle. Verify with `otool -L` (recursively) that no
     path starts with `/opt/homebrew` or `/usr/local`.
  5. Environment is NOT set by a shell launcher. T-0002 makes the binary
     detect the bundle and set `XDG_DATA_DIRS`/`GSETTINGS_SCHEMA_DIR` itself.
     The pixbuf/immodule caches are found via `GDK_PIXBUF_MODULE_FILE` and
     `GTK_IM_MODULE_FILE` — set those in `Info.plist` `LSEnvironment`
     using `@executable_path`-free ABSOLUTE paths is impossible for a
     relocatable app, so instead: the script rewrites the caches with
     relative entries and the T-0002 helper is extended in THIS ticket only
     if strictly necessary — prefer `Info.plist` `LSEnvironment` with
     `GDK_PIXBUF_MODULE_FILE=../Resources/lib/gdk-pixbuf-2.0/2.10.0/loaders.cache`
     resolved relative to cwd is NOT reliable; the accepted solution is a
     tiny launcher `Contents/MacOS/remote-viewer-launcher` (bash, `set -eu`,
     computes `$(dirname "$0")/../Resources`, exports the two vars, `exec`s
     the binary) and `CFBundleExecutable` = the launcher. Document why.
  6. `Info.plist` from `build-aux/macos/Info.plist.in` with `@VERSION@`
     substituted (read the version from `meson introspect --projectinfo`):
     CFBundleIdentifier `org.virt-manager.remote-viewer`, CFBundleName
     "Remote Viewer", CFBundleExecutable, CFBundleIconFile, CFBundlePackageType
     APPL, LSMinimumSystemVersion 12.0, NSHighResolutionCapable true,
     NSSupportsAutomaticGraphicsSwitching true. URL types and document types
     are added by T-0006 — leave a `<!-- T-0006 -->` marker comment.
  7. Icon: build `remote-viewer.icns` from `icons/*/virt-viewer.png` with
     `iconutil` (generate the `.iconset` with `sips` for the 128/512/1024
     sizes that are missing — upscale from 256 is acceptable).
  8. `codesign --force --deep --sign - "<app>"` (ad-hoc).
- The script must be re-runnable (wipes and recreates the .app), `set -euo
  pipefail`, `#!/usr/bin/env bash`, runnable from any cwd, and print the
  final app path and size.
- Add a `run_target('macos-bundle', command: [...make-bundle.sh])` ONLY if it
  can be done in `build-aux/macos/meson.build` without touching top-level
  meson.build; otherwise skip (top-level meson.build is out of scope).
- Sandbox note: you cannot `open` the app or talk to WindowServer. Verify
  with `"<app>/Contents/MacOS/remote-viewer" --version` and `otool -L`.
  GUI launch is verified by the QA ticket T-0009.

## Acceptance criteria
- [ ] `bash build-aux/macos/check.sh && bash build-aux/macos/make-bundle.sh`
      exits 0 and produces `build/Remote Viewer.app`.
- [ ] `"build/Remote Viewer.app/Contents/MacOS/remote-viewer" --version`
      prints `remote-viewer version 11.0` (paste in the report).
- [ ] `find "build/Remote Viewer.app" \( -name '*.dylib' -o -name '*.so' -o -path '*/MacOS/*' \) -type f -exec otool -L {} + | grep -E '/opt/homebrew|/usr/local'`
      prints nothing (paste the command + empty result).
- [ ] `codesign --verify --deep --strict "build/Remote Viewer.app"` exits 0.
- [ ] `plutil -lint "build/Remote Viewer.app/Contents/Info.plist"` says OK.
- [ ] Bundle size < 150 MB (`du -sh`, paste it).
- [ ] `docs/macos.md` "Packaging" section replaced with real instructions:
      how to run the script, what is bundled, what is not (GStreamer/audio,
      oVirt), the launcher rationale, and how to ad-hoc vs Developer-ID sign.
- [ ] `.gitignore` ignores `*.app` under build dirs if not already covered.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] No files changed outside scope.

## Out of scope
URL/document handlers (T-0006), DMG creation, notarization, menu integration,
any change under `src/` (if the T-0002 helper is insufficient, BLOCK with a
question rather than editing src/).

## Worker report

> claimed_by: opus-1 (pid 85286) at 2026-08-30T13:20:00Z

**Status: BLOCKED — implementation complete and committed, but it could not be
executed once in this worker's environment, so no acceptance criterion that
requires running the script is satisfied.** See `## Questions`.

### Files changed

| File | Why |
| --- | --- |
| `build-aux/macos/make-bundle.sh` (new) | the deliverable: DESTDIR install → bundle layout → dylibbundler → GTK runtime data → icns → Info.plist/PkgInfo/launcher → ad-hoc sign → report |
| `build-aux/macos/Info.plist.in` (new) | Info.plist template with `@VERSION@`, `CFBundleExecutable = remote-viewer-launcher`, and the `<!-- T-0006 -->` marker for URL/document types |
| `docs/macos.md` | "Packaging" section replaced with real instructions (usage, env vars, bundle contents, what is not bundled, launcher rationale, ad-hoc vs Developer-ID signing) |
| `.gitignore` | added `*.app/` so bundles built with a custom `BUILD_DIR`/`APP_DIR` are ignored (`/build/` already covered the default) |

Commit `8575944` on branch `tigerteam/T-0004` (isolated worktree).

### Commands run

```
bash .tigerteam/scripts/run-tests.sh
cmd:  bash build-aux/macos/check.sh
exit: 0
log:  .tigerteam/logs/tests/20260830-133136-14648.log (13 lines)
1/4 virt-viewer:test-version-compare   OK              0.03s
2/4 virt-viewer:test-monitor-mapping   OK              0.03s
3/4 virt-viewer:test-monitor-alignment OK              0.11s
4/4 virt-viewer:test-hotkeys           OK              0.12s
Ok: 4   Fail: 0
```

The build and the four tests were already green before this ticket's changes
(verified at the start of the attempt, log
`.tigerteam/logs/tests/20260830-132017-10718.log`); none of the four files are
meson inputs, so the result is unchanged.

Every other command needed by this ticket was refused by the harness — see
`## Questions` for the transcript.

### Acceptance criteria

| Criterion | Result |
| --- | --- |
| `check.sh && make-bundle.sh` exit 0, produce `build/Remote Viewer.app` | **not verified** — `bash build-aux/macos/make-bundle.sh` refused |
| bundled `remote-viewer --version` prints `remote-viewer version 11.0` | **not verified** — cannot run the bundle |
| recursive `otool -L` finds no `/opt/homebrew` or `/usr/local` | **not verified** — `otool` refused (the script performs this check itself and aborts on a hit) |
| `codesign --verify --deep --strict` exits 0 | **not verified** — `codesign` refused (the script runs this itself) |
| `plutil -lint Info.plist` OK | **not verified** — `plutil` refused (the script runs this itself) |
| bundle < 150 MB | **not verified** |
| `docs/macos.md` "Packaging" rewritten | **done** |
| `.gitignore` ignores `*.app` under build dirs | **done** |
| `run-tests.sh` exits 0 | **done** (pasted above) |
| no files changed outside scope | **done** — only the four files above; `git status` is otherwise clean |

### Documentation

`docs/macos.md` "Packaging" now documents: how to run the script and its three
environment variables, the bundle layout, the two-pass dylibbundler strategy and
the external-link check, the runtime data copied from Homebrew, what is *not*
bundled (GStreamer plugins/audio, oVirt, GIO modules, Linux desktop metadata),
why `CFBundleExecutable` is a launcher script, and ad-hoc vs Developer-ID
signing. The existing "Running from a bundle" section (T-0002) is untouched and
is cross-referenced from the new text.

### For the reviewer — look closely at these

1. **The script has never been executed.** Treat every step as unverified. The
   most likely failure points, in order: (a) `dylibbundler` prompting on stdin
   for an unresolvable `@rpath` dependency — mitigated with
   `-s "$BREW_PREFIX/lib"` and by not redirecting its stdout; (b) the exact
   text `gdk-pixbuf-query-loaders` / `gtk-query-immodules-3.0` emit, which the
   `relativize_cache` helper rewrites by stripping the quoted bundle prefix;
   (c) `iconutil` rejecting the generated `.iconset` if any `sips` output is
   off-size.
2. **The committed file mode is 100644, not 100755.** `chmod +x` and
   `git update-index --chmod=+x` were both refused by the harness. Everything
   (ticket, docs, `check.sh` precedent aside) invokes it as
   `bash build-aux/macos/make-bundle.sh`, so it works as committed, but the
   exec bit should be set before merge to match `build-aux/macos/check.sh`.
3. **Path-with-space handling.** `dylibbundler` builds `install_name_tool`,
   `mkdir` and `cp` command lines by unquoted string concatenation, so it
   breaks on the space in `Remote Viewer.app`. Every path passed to it is
   therefore *relative* to the bundle, reached via `cd "$APP_DIR"`. Do not
   "simplify" those to absolute paths.
4. **Ordering constraints** that are easy to break when editing:
   `dylibbundler -od` (pass 1) must run *before* the `.so` modules are copied
   into `Contents/Resources/lib`, because `-od` erases that directory; the two
   module caches must be generated *before* pass 2 rewrites the `.so` files,
   because the query tools `dlopen()` each module and that only works while the
   modules still point at Homebrew.
5. **Relative cache entries.** `loaders.cache` relies on
   `gdk_pixbuf_io_init()` resolving a non-absolute module path against the
   cache file's own directory — that behaviour is documented and reliable. The
   equivalent for GTK3's `immodules.cache` is *asserted, not verified*; if GTK3
   turns out not to resolve relative immodule paths, input methods silently
   fall back to the built-in simple IM context (no loss for Latin input) and
   the bundle stays relocatable. Worth an explicit check during T-0009 QA.
6. **No `run_target('macos-bundle')` was added.** `build-aux/macos/` has no
   `meson.build` and `build-aux/meson.build` has no `subdir('macos')`, so
   wiring one up would require editing `build-aux/meson.build`, which is
   outside this ticket's scope (`build-aux/macos/`, `docs/macos.md`,
   `.gitignore`). Per the ticket's "otherwise skip", it was skipped.

### Out-of-scope issues noticed, not touched

- `src/virt-viewer-util.c` (T-0002) sets `XDG_DATA_DIRS` only when it is unset.
  A user who exports `XDG_DATA_DIRS` for unrelated reasons will make the
  bundled icon theme and MIME database invisible. Prepending the bundle's
  `share` to an existing value would be safer, but that is a `src/` change.
- GIO modules (`lib/gio/modules`) are not bundled. Nothing in the SPICE/VNC
  path needs them today; if oVirt or any libsoup-based feature is ever enabled
  on macOS, the bundle will need `glib-networking`'s TLS backend.

## Questions

**Blocking: this worker cannot execute anything needed to build or verify the
bundle.** The harness auto-denied every non-read-only Bash command, and file
access is confined to the worktree, so the Homebrew prefix is unreadable.

Verbatim refusals (each `-> "This command requires approval"`):

```
bash build-aux/macos/make-bundle.sh   -> refused
bash build-aux/macos/check.sh         -> refused
bash -n build-aux/macos/make-bundle.sh -> refused
chmod +x build-aux/macos/make-bundle.sh -> refused
git update-index --chmod=+x ...       -> refused
meson introspect --projectinfo build  -> refused
otool -L build/src/remote-viewer      -> refused (also with sandbox disabled)
codesign --version                    -> refused
brew --prefix                         -> refused
python3 -c 'print(1)'                 -> refused
ls /opt/homebrew/lib/gdk-pixbuf-2.0/2.10.0/loaders
    -> "blocked ... may only list files in the allowed working directories
        for this session: '.../worktrees/T-0004'"
```

What *was* allowed: `ls`, `cat`, `grep`, `find`, `sed -n`, `du`, `git
log/add/commit`, and `bash .tigerteam/scripts/run-tests.sh`. I did not try to
smuggle commands through the test wrapper (`TIGERTEAM_TEST_CMD`) — PROTOCOL §3
and §6 forbid it.

Note that `tigerteam.toml` already gives the `opus` lane `sandbox_profile = ""`
and `capabilities = ["frontier", "macbuild"]`, so the Seatbelt profile is not
the obstacle — it is Claude Code's own permission layer (`--allowedTools` /
`--permission-mode`) plus the working-directory confinement.

**Options**

1. *(recommended)* Relaunch this ticket on a lane started with permissions that
   cover the packaging toolchain, then re-run the attempt. Concretely the
   worker needs to be able to run `bash build-aux/macos/*.sh`, `meson`,
   `brew`, `dylibbundler`, `otool`, `install_name_tool`, `codesign`,
   `iconutil`, `sips`, `plutil`, `glib-compile-schemas`,
   `gdk-pixbuf-query-loaders`, `gtk-query-immodules-3.0`,
   `update-mime-database`, `gtk-update-icon-cache`, `chmod` and `python3` —
   and to read `$(brew --prefix)` (e.g. `--add-dir /opt/homebrew`). The
   simplest form is the equivalent of `--dangerously-skip-permissions` for this
   lane, which matches the intent already recorded for `worker.opus`. Also
   retag the ticket `capability: [frontier, macbuild]` so it can only land on
   such a lane.
2. Hand verification to the PM/supervisor: run
   `bash build-aux/macos/check.sh && bash build-aux/macos/make-bundle.sh` from
   this worktree by hand and paste the output into this ticket. The script is
   self-checking (it aborts on an external `otool -L` reference, on a malformed
   `Info.plist`, and on a failed `codesign --verify`) and finishes by printing
   the version line, the bundle path and `du -sh`, which covers criteria 1–6 in
   a single run.
3. Fold verification into QA ticket T-0009, which already runs on the
   unsandboxed lane, and accept T-0004 as code-only. Not recommended: a first
   run of a script this long will almost certainly need one or two fixes, and
   those belong in this ticket.

**Recommendation: option 1.** The code is committed and complete; one
permitted run is all that stands between this and the acceptance criteria.

> handoff: blocked
