---
id: T-0022
title: Native macOS SPICE viewer PoC — spice-glib + CALayer/Metal (screen, keyboard, mouse)
priority: P1
complexity: C3
depends_on: []
scope: [macos-native/]
capability: [macbuild]
---

## Goal
Milestone 1 of a native macOS SPICE client, no GTK anywhere: a standalone
app that connects to a `spice://host:port` URI via spice-client-glib,
renders the guest framebuffer in an NSWindow at native Retina scale, and
forwards keyboard and absolute-mode mouse input. User decision 2026-09-01:
this is the new primary direction; screen + keyboard + basic mouse is the
whole milestone. NO clipboard, NO file transfer, NO USB, NO relative/server
mouse mode (log-and-ignore is fine), single display only.

## Context
- spice-client-glib 0.42 is installed (pkg-config `spice-client-glib-2.0`);
  its unpacked source (for API reference) is in the PM scratchpad at
  `/private/tmp/claude-501/-Users-d-b-virt-viewer/48736c42-ecf1-4803-a3b8-b5738f424da3/scratchpad/spice-gtk-0.42/`.
- Architecture (the CocoaSpice-proven shape): run the GLib main context on
  a dedicated background thread (never block the AppKit main thread);
  marshal framebuffer updates to the main thread (dispatch_async) and
  input events back to the GLib thread. Display channel: react to
  `display-primary-create` (format/width/height/stride/data) and
  `display-invalidate` (damage rects) — upload damage to a CALayer-backed
  view (IOSurface/Metal texture or layer contents; your choice, justify
  briefly) with `contentsScale = backingScaleFactor` so Retina is sharp,
  no interpolation blur. Inputs channel: `spice_inputs_*` for key
  press/release (XT scancodes), absolute position, buttons, scroll.
- Keyboard: generate the osx→xtkbd table from keycodemapdb — the same
  database qemu and spice-gtk use. It is vendored in the unpacked
  spice-gtk source (`subprojects/keycodemapdb`, `tools/keymap-gen`);
  generate a C table (stdc lang) mapping `NSEvent.keyCode` → XT scancode.
  Modifiers do NOT arrive as keyDown/keyUp on macOS: synthesize
  press/release from `flagsChanged` bitmask deltas (track previous flags).
  Release all pressed keys on window/app deactivation.
- Language/build: Objective-C (+ the C table), built standalone by a small
  script or Makefile inside `macos-native/` — do NOT touch the project's
  meson build. Ad-hoc codesign if needed for local run.
- **Test target ladder** (no SPICE server exists on this host yet):
  1. `brew install spice-server` — probably absent; fail fast.
  2. Build spice-server from source (gitlab.freedesktop.org/spice/spice)
     with minimal options; its test tools (e.g. `test-display-no-ssl` /
     the test-display-base helpers) provide a synthetic animated display
     server on localhost. Timebox this to ~25 minutes.
  3. If both fail: commit what you have, land the ticket INCOMPLETE-style
     with a `## Questions` block asking the PM for a reachable
     `spice://` test URI (the user has real SPICE guests).
- **Engine time ceiling is real (~45 min wall; two prior tickets died at
  it).** Commit early and often on your branch; if you approach ~40
  minutes, hand off INCOMPLETE with precise next steps rather than
  pushing through. A multi-attempt relay is expected and fine.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (main build untouched).
- [ ] `macos-native/` builds with one documented command (README.md with
      build + run instructions and the dependency list).
- [ ] Against the test target: live framebuffer visible, damage-driven
      updates (not full-frame repaints), sharp at Retina scale; window
      title set from the URI/guest; ⌘Q exits cleanly (all keys released,
      channels disconnected).
- [ ] Keyboard: scancodes from the generated keycodemapdb table reach the
      inputs channel, including flagsChanged-synthesized modifiers —
      evidence via guest echo if the target renders typing, else via
      logged scancode stream for a scripted key sequence.
- [ ] Mouse: absolute position + left/right buttons + scroll wheel reach
      the inputs channel (same evidence standard).
- [ ] Report: what works, LoC count, subjective rendering/latency notes vs
      the GTK viewer, and a concrete gap list to daily-driveable (with
      rough sizes: relative mouse, clipboard, multi-display, dialogs).
- [ ] Screenshot evidence preserved as artifacts.
- [ ] No files changed outside `macos-native/`; zero clipboard/file/USB
      code.

## Out of scope
Everything past milestone 1. Touching virt-viewer sources, docs/, or the
GTK bugs. Swift, Xcode project files, App Store packaging.

## Worker report
(left empty — the worker fills this)
